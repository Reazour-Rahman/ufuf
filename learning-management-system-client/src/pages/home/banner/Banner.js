import { Box, Typography } from '@mui/material';
import React from 'react';

const Banner = () => {
    return (
        <Box>
            <Typography variant="bigHead">This is Banner dear</Typography>
        </Box>
    );
};

export default Banner;